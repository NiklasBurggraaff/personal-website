---
type Props = {
    src: string;
    caption: string;
};

const { src, caption } = Astro.props;
---

<figure class="clear-both !m-4 sm:float-right sm:m-4 sm:w-1/3">
    <img {src} alt={caption} />
    <figcaption aria-hidden="true">{caption}</figcaption>
</figure>
